# 咨询师管理模块使用说明

## 功能概述

咨询师管理模块负责心理咨询师的注册、资料维护、排班管理、预约处理和统计数据展示等功能。

## 主要功能

### 1. 咨询师注册
- **注册方式**: 手机号 + 邮箱注册
- **必填信息**: 姓名、职称、手机号、密码、邮箱、从业年限
- **可选信息**: 个人简介、擅长领域、咨询方法、资质认证
- **初始状态**: 注册成功后状态为正常，可立即使用

### 2. 个人资料管理
- **查看资料**: 姓名、职称、联系方式、简介、擅长领域、统计数据
- **编辑资料**: 更新个人信息、标签、方法、头像
- **统计展示**: 评分、咨询时长、帮助人数、好评率

### 3. 排班管理
- **周视图**: 查看当前周或指定周的排班
- **批量设置**: 设置每天各时间段的可用性
- **占用保护**: 已被预约的时间段自动标记为占用

### 4. 预约处理
- **今日预约**: 快速查看今日预约列表
- **全部预约**: 分页查看所有预约，支持状态筛选
- **确认预约**: 将待处理预约确认，可填写咨询地点
- **拒绝预约**: 拒绝预约并填写原因
- **完成预约**: 标记预约完成，可填写备注

### 5. 数据统计
- **今日统计**: 今日预约数、已完成数、待处理数、未读消息数
- **累计数据**: 总咨询时长、帮助人数、评分、好评率

## 技术实现

### 后端实现

#### 1. 实体类
- `Doctor`: 咨询师实体（id, name, title, avatar, password, phone, email, gender, years, description, tags, methods, certifications, rating, totalHours, helpedCount, positiveRate, location, available, status）

#### 2. Mapper层
- `DoctorMapper`: 咨询师数据访问
  - `findById(Long id)`: 根据ID查询
  - `findByPhone(String phone)`: 根据手机号查询
  - `findByEmail(String email)`: 根据邮箱查询
  - `findByPhoneOrEmail(String username)`: 根据手机号或邮箱查询（登录用）
  - `findAllAvailable()`: 查询所有可用咨询师
  - `searchByKeyword(String keyword)`: 关键字搜索
  - `insert(Doctor doctor)`: 新增
  - `update(Doctor doctor)`: 更新
  - `updateAvatar(Long doctorId, String avatar)`: 更新头像
  - `updateStats(Long doctorId, ...)`: 更新统计数据

#### 3. DTO类
- `DoctorDTO`: 咨询师信息传输对象
- `DoctorRegisterRequest`: 注册请求
- `DoctorRegisterResponse`: 注册响应
- `UpdateDoctorProfileRequest`: 更新资料请求
- `DoctorTodayStatsDTO`: 今日统计数据
- `DoctorStatsDTO`: 累计统计数据

#### 4. 服务层
`DoctorService` 接口和 `DoctorServiceImpl` 实现类：
- `register(DoctorRegisterRequest request)`: 咨询师注册
- `getDoctorById(Long doctorId)`: 根据ID获取咨询师
- `getDoctorProfile(Long doctorId)`: 获取咨询师资料
- `updateDoctorProfile(Long doctorId, UpdateDoctorProfileRequest request)`: 更新资料
- `getTodayStats(Long doctorId)`: 获取今日统计
- `getAllAvailableDoctors()`: 获取所有可用咨询师
- `searchDoctors(String keyword)`: 搜索咨询师

#### 5. 控制器
`DoctorController` 提供咨询师端 REST API：
- `GET /api/doctor/profile`: 获取个人信息
- `PUT /api/doctor/profile`: 更新个人信息
- `GET /api/doctor/stats/today`: 获取今日统计
- `GET /api/doctor/appointments/today`: 今日预约列表
- `GET /api/doctor/appointments`: 预约列表（分页）
- `PUT /api/doctor/appointments/{id}/confirm`: 确认预约
- `PUT /api/doctor/appointments/{id}/reject`: 拒绝预约
- `PUT /api/doctor/appointments/{id}/complete`: 完成预约
- `GET /api/doctor/messages/pending`: 待回复消息
- `GET /api/doctor/chat/sessions`: 聊天会话列表
- `GET /api/doctor/chat/sessions/{session_id}/messages`: 会话消息
- `POST /api/doctor/chat/send`: 发送消息
- `GET /api/doctor/schedules`: 获取排班
- `PUT /api/doctor/schedules`: 更新排班

### 前端实现

#### DoctorRegisterView.vue
咨询师注册页面：
**核心功能**:
- 多步骤注册表单
- 基本信息、专业资质、擅长领域填写
- 标签和方法多选
- 表单验证

**主要方法**:
- `handleRegister()`: 提交注册
- `addTag()`: 添加擅长领域标签
- `addMethod()`: 添加咨询方法

#### DoctorHomeView.vue
咨询师工作台：
**核心功能**:
- 今日统计卡片展示
- 今日预约快速列表
- 待回复消息列表
- 快速导航入口

**主要方法**:
- `loadTodayStats()`: 加载今日统计
- `loadTodayAppointments()`: 加载今日预约
- `loadPendingMessages()`: 加载待回复消息

#### DoctorProfileView.vue
咨询师资料管理：
**核心功能**:
- 查看完整资料
- 在线编辑信息
- 头像上传
- 标签和方法管理

**主要方法**:
- `loadProfile()`: 加载资料
- `handleUpdate()`: 更新资料
- `handleAvatarUpload()`: 上传头像

#### DoctorScheduleView.vue
排班管理页面：
**核心功能**:
- 周视图展示排班
- 按周切换（上一周/下一周）
- 批量设置时间段可用性
- 已预约时间段标记

**主要方法**:
- `loadSchedules(weekOffset)`: 加载排班
- `toggleSlot(date, slot)`: 切换时间段状态
- `handleSave()`: 保存排班

#### DoctorAppointmentsView.vue
预约管理页面：
**核心功能**:
- 预约列表展示（分页）
- 状态筛选（全部/待处理/已确认/已完成/已取消）
- 预约详情查看
- 确认/拒绝/完成操作

**主要方法**:
- `loadAppointments()`: 加载预约列表
- `handleConfirm(id)`: 确认预约
- `handleReject(id)`: 拒绝预约
- `handleComplete(id)`: 完成预约
- `filterByStatus(status)`: 状态筛选

## 配置信息

### application.yml
```yaml
# 咨询师相关配置
doctor:
  default-rating: 5.0  # 初始评分
  default-positive-rate: 100  # 初始好评率
```

### 数据库表结构
```sql
CREATE TABLE doctor (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(50) NOT NULL COMMENT '姓名',
  title VARCHAR(50) COMMENT '职称',
  avatar VARCHAR(500) COMMENT '头像URL',
  password VARCHAR(255) NOT NULL COMMENT '密码（加密）',
  phone VARCHAR(20) UNIQUE COMMENT '手机号',
  email VARCHAR(100) UNIQUE COMMENT '邮箱',
  gender TINYINT COMMENT '性别：0-未知 1-男 2-女',
  years INT DEFAULT 0 COMMENT '从业年限',
  description TEXT COMMENT '个人简介',
  tags TEXT COMMENT '擅长领域标签（JSON数组）',
  methods VARCHAR(500) COMMENT '咨询方法（JSON数组）',
  certifications TEXT COMMENT '资质认证（JSON数组）',
  rating DECIMAL(2,1) DEFAULT 5.0 COMMENT '评分',
  total_hours INT DEFAULT 0 COMMENT '累计咨询时长',
  helped_count INT DEFAULT 0 COMMENT '帮助人数',
  positive_rate INT DEFAULT 100 COMMENT '好评率',
  location VARCHAR(200) COMMENT '线下咨询地址',
  available TINYINT DEFAULT 1 COMMENT '是否可预约',
  status TINYINT DEFAULT 1 COMMENT '状态：0-待审核 1-正常 2-下架',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

## API接口文档

### 1. 咨询师注册
**请求**: `POST /api/auth/doctor/register`  
**请求体**:
```json
{
  "name": "李华",
  "title": "注册心理师",
  "phone": "13812345678",
  "password": "password123",
  "email": "lihua@example.com",
  "years": 8,
  "description": "擅长认知行为疗法...",
  "tags": ["焦虑抑郁", "人际关系"],
  "methods": ["认知行为疗法", "人本主义疗法"],
  "certifications": ["国家二级心理咨询师"]
}
```
**响应**:
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "doctorId": 1,
    "name": "李华"
  }
}
```

### 2. 获取咨询师资料
**请求**: `GET /api/doctor/profile`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "data": {
    "id": 1,
    "name": "李华",
    "title": "注册心理师",
    "avatar": "/uploads/avatars/doctors/avatar_1.jpg",
    "phone": "13812345678",
    "email": "lihua@example.com",
    "years": 8,
    "rating": 4.9,
    "description": "擅长认知行为疗法...",
    "tags": ["焦虑抑郁", "人际关系"],
    "methods": ["认知行为疗法"],
    "certifications": ["国家二级心理咨询师"],
    "stats": {
      "totalHours": 120,
      "helpedCount": 56,
      "positiveRate": 98
    },
    "available": true
  }
}
```

### 3. 获取今日统计
**请求**: `GET /api/doctor/stats/today`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "data": {
    "todayAppointments": 5,
    "completed": 2,
    "pending": 3,
    "unreadMessages": 8
  }
}
```

### 4. 获取排班
**请求**: `GET /api/doctor/schedules?weekOffset=0`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "data": {
    "weekStart": "2025-12-23",
    "weekEnd": "2025-12-29",
    "schedules": {
      "2025-12-23": {
        "09:00": {"available": true, "booked": false},
        "10:00": {"available": true, "booked": true},
        "14:00": {"available": false, "booked": false}
      }
    }
  }
}
```

### 5. 更新排班
**请求**: `PUT /api/doctor/schedules`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "schedules": {
    "2025-12-23": {
      "09:00": true,
      "10:00": true,
      "14:00": false,
      "15:00": true
    },
    "2025-12-24": {
      "09:00": true,
      "10:00": false
    }
  }
}
```
**响应**:
```json
{
  "code": 200,
  "data": {
    "weekStart": "2025-12-23",
    "weekEnd": "2025-12-29",
    "schedules": {...}
  }
}
```

### 6. 确认预约
**请求**: `PUT /api/doctor/appointments/{id}/confirm`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "location": "心理咨询中心101室"
}
```
**响应**:
```json
{
  "code": 200,
  "data": {
    "id": 10023,
    "status": "confirmed",
    "location": "心理咨询中心101室",
    "studentName": "张同学",
    "date": "2025-12-25",
    "timeSlot": "14:00"
  }
}
```

### 7. 拒绝预约
**请求**: `PUT /api/doctor/appointments/{id}/reject`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "reason": "该时间段临时有事，抱歉"
}
```
**响应**:
```json
{
  "code": 200,
  "data": {
    "id": 10023,
    "status": "rejected",
    "rejectReason": "该时间段临时有事，抱歉"
  }
}
```

### 8. 完成预约
**请求**: `PUT /api/doctor/appointments/{id}/complete`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "notes": "咨询进展顺利，建议继续巩固"
}
```
**响应**:
```json
{
  "code": 200,
  "data": {
    "id": 10023,
    "status": "completed",
    "notes": "咨询进展顺利，建议继续巩固"
  }
}
```

## 使用流程

1. **咨询师注册**: 填写基本信息和专业资质 → 提交注册 → 注册成功
2. **登录系统**: 使用手机号或邮箱登录 → 进入咨询师工作台
3. **设置排班**: 进入排班管理 → 选择周 → 设置可用时间段 → 保存
4. **处理预约**: 查看待处理预约 → 确认或拒绝 → 与学生沟通 → 完成咨询 → 标记完成
5. **查看统计**: 工作台查看今日统计和累计数据
6. **更新资料**: 个人中心编辑资料、上传头像、更新标签

## 注意事项

1. **手机号唯一**: 每个手机号只能注册一次
2. **排班规则**: 已被预约的时间段无法修改为不可用
3. **预约状态**: 只能确认待处理(pending)的预约，只能完成已确认(confirmed)的预约
4. **统计更新**: 完成预约后自动更新咨询时长和帮助人数
5. **标签格式**: 擅长领域和咨询方法存储为JSON数组
6. **头像上传**: 咨询师头像保存在 `uploads/avatars/doctors/` 目录

## 业务规则

1. **注册规则**:
   - 手机号和邮箱必须唯一
   - 从业年限必须大于等于0
   - 初始评分为5.0，好评率为100%

2. **排班规则**:
   - 周偏移支持 -4到+4（可查看过去4周和未来4周）
   - 已被预约的时间段保持占用状态
   - 删除排班前需确保该时间段无预约

3. **预约处理规则**:
   - pending → confirmed（确认）
   - pending → rejected（拒绝）
   - confirmed → completed（完成）
   - 确认预约后时间段自动标记为已占用
   - 完成预约后更新咨询师统计数据

4. **统计计算规则**:
   - 每完成一次预约，帮助人数+1
   - 每完成一次预约，咨询时长+1小时（默认）
   - 评分和好评率由评价系统计算（未实现）

## 未来改进

1. 支持自定义时间段（当前固定为整点）
2. 添加请假功能（批量设置不可用）
3. 实现评价系统
4. 支持咨询费用设置
5. 添加咨询记录模板
6. 支持视频咨询排班

---


技术栈: Spring Boot 3.x + Vue 3 + MySQL  

