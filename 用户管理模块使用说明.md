# 用户管理模块使用说明

## 功能概述

用户管理模块是心理咨询系统的核心模块之一，负责学生用户的注册、登录、个人信息维护等基础功能。

## 主要功能

### 1. 用户注册
- **注册方式**: 学号 + 邮箱注册
- **必填信息**: 用户名、学号、密码、邮箱、学院
- **密码加密**: 使用 BCrypt 加密存储
- **唯一性校验**: 学号和邮箱不可重复

### 2. 用户登录
- **登录方式**: 支持学号或邮箱登录
- **角色支持**: student（学生）、doctor（咨询师）
- **令牌生成**: 登录成功后返回 JWT Token
- **状态校验**: 禁用账号无法登录

### 3. 个人信息管理
- **查看信息**: 获取当前用户详细资料
- **更新资料**: 昵称、学院、班级、手机号、邮箱、头像
- **密码修改**: 需验证旧密码，新密码至少6位
- **头像上传**: 支持 JPG/PNG/GIF/WEBP，最大5MB

## 技术实现

### 后端实现

#### 1. 实体类
- `User`: 用户实体（id, username, studentId, password, email, college, majorClass, phone, avatar, status, checkinStreak, counselingHours, appointmentsCount, allowPeerView, allowNotification）

#### 2. Mapper层
- `UserMapper`: 用户数据访问
  - `findById(Long id)`: 根据ID查询用户
  - `findByStudentIdOrEmail(String username)`: 根据学号或邮箱查询
  - `countByStudentId(String studentId)`: 检查学号是否存在
  - `countByEmail(String email)`: 检查邮箱是否存在
  - `insert(User user)`: 新增用户
  - `update(User user)`: 更新用户信息
  - `updatePassword(Long userId, String password)`: 修改密码
  - `updateAvatar(Long userId, String avatar)`: 更新头像
  - `updateCheckinStreak(Long userId, Integer streak)`: 更新签到连续天数

#### 3. DTO类
- `UserDTO`: 用户信息传输对象
- `RegisterRequest`: 注册请求
- `LoginRequest`: 登录请求
- `LoginResponse`: 登录响应（包含 token、user、role）
- `RegisterResponse`: 注册响应
- `UpdateProfileRequest`: 更新资料请求

#### 4. 工具类
- `PasswordUtil`: 密码加密工具
  - `encode(String rawPassword)`: 加密密码
  - `matches(String rawPassword, String encodedPassword)`: 验证密码
- `JwtUtil`: JWT 令牌工具
  - `generateToken(Long userId, String username)`: 生成令牌
  - `getUserIdFromToken(String token)`: 从令牌获取用户ID
- `UserContext`: 用户上下文工具
  - `getCurrentUserId(HttpServletRequest request)`: 获取当前登录用户ID
  - `getCurrentRole(HttpServletRequest request)`: 获取当前用户角色

#### 5. 服务层
`UserService` 接口和 `UserServiceImpl` 实现类：
- `register(RegisterRequest request)`: 用户注册
- `login(LoginRequest request)`: 用户登录（支持学生和咨询师）
- `getProfile(Long userId)`: 获取个人信息
- `updateProfile(Long userId, UpdateProfileRequest request)`: 更新个人信息
- `changePassword(Long userId, String oldPassword, String newPassword)`: 修改密码
- `updateCheckinStreak(Long userId, Integer streak)`: 更新签到天数
- `getUserById(Long userId)`: 根据ID获取用户

#### 6. 控制器
`AuthController` 提供认证 REST API：
- `POST /api/auth/register`: 用户注册
- `POST /api/auth/login`: 用户登录
- `POST /api/auth/doctor/register`: 咨询师注册

`UserController` 提供用户管理 REST API：
- `GET /api/user/profile`: 获取当前用户信息
- `PUT /api/user/profile`: 更新用户信息
- `PUT /api/user/password`: 修改密码

### 前端实现

#### RegisterView.vue
学生注册页面：
**核心功能**:
- 表单验证（用户名、学号、邮箱、密码、学院必填）
- 密码长度校验（至少6位）
- 注册成功后自动跳转登录页

**主要方法**:
- `handleRegister()`: 提交注册表单
- `goToLogin()`: 跳转到登录页

#### LoginView.vue
用户登录页面：
**核心功能**:
- 支持学号或邮箱登录
- 记住密码（可选）
- 根据角色跳转不同首页

**主要方法**:
- `handleLogin()`: 提交登录
- `goToRegister()`: 跳转注册页
- `goToDoctorRegister()`: 跳转咨询师注册页

#### StudentProfileView.vue
个人信息管理页面：
**核心功能**:
- 展示用户资料
- 在线编辑（昵称、学院、班级、联系方式等）
- 头像上传裁剪
- 修改密码

**主要方法**:
- `loadProfile()`: 加载用户信息
- `handleUpdate()`: 更新用户信息
- `handleAvatarUpload()`: 上传头像
- `handlePasswordChange()`: 修改密码

## 配置信息

### application.yml
```yaml
# JWT配置
jwt:
  secret: your-secret-key-here
  expiration: 86400000  # 24小时（毫秒）

# 文件上传配置
file:
  upload-dir: uploads  # 上传文件保存目录
```

### 数据库表结构
```sql
CREATE TABLE user (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  username VARCHAR(50) NOT NULL COMMENT '用户名',
  student_id VARCHAR(20) NOT NULL UNIQUE COMMENT '学号',
  password VARCHAR(255) NOT NULL COMMENT '密码（加密）',
  email VARCHAR(100) NOT NULL UNIQUE COMMENT '邮箱',
  college VARCHAR(100) COMMENT '学院',
  major_class VARCHAR(100) COMMENT '专业班级',
  phone VARCHAR(20) COMMENT '手机号',
  avatar VARCHAR(500) COMMENT '头像URL',
  status TINYINT DEFAULT 1 COMMENT '状态：0-禁用 1-正常',
  checkin_streak INT DEFAULT 0 COMMENT '连续签到天数',
  counseling_hours DECIMAL(10,2) DEFAULT 0 COMMENT '咨询时长',
  appointments_count INT DEFAULT 0 COMMENT '预约次数',
  allow_peer_view TINYINT DEFAULT 1 COMMENT '允许同辈查看',
  allow_notification TINYINT DEFAULT 1 COMMENT '允许通知',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

## API接口文档

### 1. 用户注册
**请求**: `POST /api/auth/register`  
**请求体**:
```json
{
  "username": "张三",
  "studentId": "2023001001",
  "password": "password123",
  "email": "zhangsan@example.com",
  "college": "计算机学院"
}
```
**响应**:
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "userId": 1
  }
}
```

### 2. 用户登录
**请求**: `POST /api/auth/login`  
**请求体**:
```json
{
  "username": "2023001001",
  "password": "password123",
  "role": "student"
}
```
**响应**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "name": "张三",
      "avatar": "/uploads/avatars/students/avatar_1.jpg",
      "college": "计算机学院"
    },
    "role": "student"
  }
}
```

### 3. 获取用户信息
**请求**: `GET /api/user/profile`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 1,
    "name": "张三",
    "studentId": "2023001001",
    "email": "zhangsan@example.com",
    "college": "计算机学院",
    "majorClass": "软件工程2301",
    "phone": "13812345678",
    "avatar": "/uploads/avatars/students/avatar_1.jpg",
    "checkinStreak": 5,
    "counselingHours": 2.5,
    "appointmentsCount": 3
  }
}
```

### 4. 更新用户信息
**请求**: `PUT /api/user/profile`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "name": "张三",
  "college": "计算机学院",
  "majorClass": "软件工程2301",
  "phone": "13812345678",
  "email": "zhangsan@example.com",
  "avatar": "/uploads/avatars/students/avatar_1.jpg"
}
```
**响应**:
```json
{
  "code": 200,
  "message": "更新成功"
}
```

### 5. 修改密码
**请求**: `PUT /api/user/password`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "oldPassword": "password123",
  "newPassword": "newpassword456"
}
```
**响应**:
```json
{
  "code": 200,
  "message": "密码修改成功"
}
```

## 使用流程

1. **新用户注册**: 填写用户名、学号、密码、邮箱、学院 → 提交注册 → 注册成功
2. **用户登录**: 输入学号/邮箱和密码 → 选择角色（学生） → 登录成功 → 跳转学生首页
3. **查看资料**: 进入个人中心 → 查看个人信息和统计数据
4. **编辑资料**: 点击编辑 → 修改昵称、学院、班级、联系方式 → 保存
5. **上传头像**: 点击头像 → 选择图片 → 裁剪调整 → 上传
6. **修改密码**: 进入密码修改 → 输入旧密码和新密码 → 提交修改

## 注意事项

1. **密码安全**: 密码使用 BCrypt 加密，不可逆，忘记密码需通过邮箱找回
2. **学号唯一性**: 每个学号只能注册一次，不可重复
3. **邮箱校验**: 邮箱需符合格式规范，用于接收系统通知
4. **头像限制**: 仅支持图片格式（JPG/PNG/GIF/WEBP），大小不超过5MB
5. **令牌有效期**: JWT Token 默认有效期24小时，过期需重新登录
6. **角色隔离**: 学生和咨询师使用不同的注册和登录流程

## 业务规则

1. **注册规则**:
   - 学号必须唯一
   - 邮箱必须唯一
   - 密码至少6位
   - 学院为必填项

2. **登录规则**:
   - 支持学号或邮箱登录
   - 禁用状态的账号无法登录
   - 登录失败返回统一提示，不暴露具体原因

3. **修改密码规则**:
   - 必须验证旧密码
   - 新密码不能与旧密码相同
   - 新密码长度至少6位

4. **头像上传规则**:
   - 学生头像保存在 `uploads/avatars/students/` 目录
   - 咨询师头像保存在 `uploads/avatars/doctors/` 目录
   - 上传成功后自动更新数据库中的头像URL

## 未来改进

1. 支持第三方登录（微信、QQ）
2. 添加邮箱验证码注册
3. 实现找回密码功能
4. 支持实名认证
5. 添加账号注销功能
6. 支持多端登录检测

---


技术栈: Spring Boot 3.x + Vue 3 + MySQL + JWT  

