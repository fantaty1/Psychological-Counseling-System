# 管理员和公告模块使用说明

## 功能概述

管理员模块提供系统后台管理功能，包括学生账号管理、咨询师账号管理和中心公告管理。管理员可以查看、搜索、审核、启用/禁用、删除用户账号，以及发布和管理全站公告。

## 主要功能

### 1. 管理员认证
- **隐藏登录入口**: 独立的管理员登录页面，不在常规导航中显示
- **JWT认证**: 基于JWT的token验证机制
- **身份验证**: 用户名密码登录
- **会话管理**: token存储与自动过期

### 2. 学生账号管理
- **列表查看**: 查看所有注册学生用户
- **搜索功能**: 按用户名、学号、邮箱关键词搜索
- **状态管理**: 启用/禁用学生账号（status: 0-禁用, 1-正常）
- **账号删除**: 物理删除学生数据及关联信息

### 3. 咨询师账号管理
- **列表查看**: 查看所有注册咨询师（包括待审核）
- **搜索功能**: 按姓名、职称、专长关键词搜索
- **审核管理**: 审核通过新注册的咨询师
- **状态管理**: 上架/下架咨询师（status: 0-下架, 1-上架, 2-待审核）
- **账号删除**: 物理删除咨询师数据及关联信息

### 4. 公告管理
- **公告发布**: 创建新的中心公告
- **公告编辑**: 修改已有公告内容
- **状态控制**: 发布/撤回公告（status: 0-草稿, 1-已发布）
- **类型分类**: 通知、紧急、维护等类型
- **优先级设置**: 控制公告显示顺序
- **公告删除**: 删除公告记录
- **前端展示**: 学生端首页显示已发布公告

## 技术实现

### 后端实现

#### 1. 实体类
- `Admin`: 管理员实体
  - id: 主键
  - username: 用户名（唯一）
  - password: 密码（BCrypt加密）
  - email: 邮箱
  - createdAt: 创建时间
  - updatedAt: 更新时间

- `Announcement`: 公告实体
  - id: 主键
  - title: 公告标题
  - content: 公告内容（支持富文本）
  - type: 公告类型（notice/urgent/maintenance）
  - priority: 优先级（数字越大越靠前）
  - status: 状态（0-草稿, 1-已发布）
  - publishedAt: 发布时间
  - createdAt: 创建时间
  - updatedAt: 更新时间

#### 2. Mapper层
- `AdminMapper`: 管理员数据访问接口
  - `findByUsername(String username)`: 根据用户名查询管理员
  - `findById(Long id)`: 根据ID查询管理员
  - `insert(Admin admin)`: 插入管理员记录
  - `update(Admin admin)`: 更新管理员信息

- `AnnouncementMapper`: 公告数据访问接口
  - `findAll()`: 查询所有公告
  - `findPublished()`: 查询已发布公告
  - `findPublishedWithLimit(int limit)`: 限制数量查询已发布公告
  - `findById(Long id)`: 根据ID查询公告
  - `insert(Announcement announcement)`: 插入公告
  - `update(Announcement announcement)`: 更新公告
  - `updateStatus(Long id, Integer status)`: 更新公告状态
  - `deleteById(Long id)`: 删除公告

#### 3. DTO类
- `AdminDTO`: 管理员数据传输对象（隐藏密码字段）
- `AdminLoginRequest`: 管理员登录请求（username, password）
- `AdminLoginResponse`: 管理员登录响应（token, adminInfo）
- `AnnouncementDTO`: 公告数据传输对象
- `AnnouncementRequest`: 公告创建/更新请求

#### 4. 服务层
**AdminService** 接口和 **AdminServiceImpl** 实现类：
- `login(AdminLoginRequest request)`: 管理员登录验证，返回JWT token
- `getAdminInfo(Long adminId)`: 获取管理员个人信息
- `getAllStudents()`: 获取所有学生用户列表
- `searchStudents(String keyword)`: 按关键词搜索学生（用户名/学号/邮箱）
- `updateStudentStatus(Long userId, Integer status)`: 更新学生账号状态
- `deleteStudent(Long userId)`: 物理删除学生账号
- `getAllDoctors()`: 获取所有咨询师列表
- `searchDoctors(String keyword)`: 按关键词搜索咨询师（姓名/职称/专长）
- `updateDoctorStatus(Long doctorId, Integer status)`: 更新咨询师状态（审核/上下架）
- `deleteDoctor(Long doctorId)`: 物理删除咨询师账号

**AnnouncementService** 接口和 **AnnouncementServiceImpl** 实现类：
- `getAllAnnouncements()`: 获取所有公告（管理员后台用）
- `getPublishedAnnouncements()`: 获取所有已发布公告
- `getPublishedAnnouncements(int limit)`: 限制数量获取已发布公告
- `getAnnouncementById(Long id)`: 获取公告详情
- `createAnnouncement(AnnouncementRequest request)`: 创建新公告
- `updateAnnouncement(AnnouncementRequest request)`: 更新公告信息
- `updateAnnouncementStatus(Long id, Integer status)`: 更新公告状态（发布/撤回）
- `deleteAnnouncement(Long id)`: 删除公告

#### 5. 控制器
**AdminController** 提供管理员 REST API：

**认证接口**:
- `POST /api/admin/auth/login`: 管理员登录
- `GET /api/admin/profile`: 获取当前管理员信息

**学生管理接口**:
- `GET /api/admin/students`: 获取所有学生列表
- `GET /api/admin/students/search`: 搜索学生（query参数：keyword）
- `PUT /api/admin/students/{id}/status`: 更新学生状态（body: {status: 0或1}）
- `DELETE /api/admin/students/{id}`: 删除学生

**咨询师管理接口**:
- `GET /api/admin/doctors`: 获取所有咨询师列表
- `GET /api/admin/doctors/search`: 搜索咨询师（query参数：keyword）
- `PUT /api/admin/doctors/{id}/status`: 更新咨询师状态（body: {status: 0/1/2}）
- `DELETE /api/admin/doctors/{id}`: 删除咨询师

**公告管理接口**:
- `GET /api/admin/announcements`: 获取所有公告
- `GET /api/admin/announcements/{id}`: 获取公告详情
- `POST /api/admin/announcements`: 创建公告
- `PUT /api/admin/announcements/{id}`: 更新公告
- `PUT /api/admin/announcements/{id}/status`: 更新公告状态（body: {status: 0或1}）
- `DELETE /api/admin/announcements/{id}`: 删除公告

**公共接口**:
- `GET /api/admin/public/announcements`: 获取已发布公告（query参数：limit，默认10）

### 前端实现

#### AdminLoginView.vue
管理员登录页面：
**核心功能**:
- 隐藏入口设计（不在常规导航显示）
- 用户名密码表单验证
- JWT token接收与存储
- 登录成功跳转到管理后台

**主要方法**:
- `handleLogin()`: 处理登录表单提交
- `setAdminToken(token)`: 存储管理员token到localStorage

#### AdminHomeView.vue
管理后台主页：
**核心功能**:
- 左侧菜单导航（学生管理、医生管理、公告管理）
- 学生列表展示与操作
- 咨询师列表展示与操作
- 公告列表展示与CRUD操作
- 搜索功能
- 状态切换按钮
- 删除确认对话框

**主要方法**:
- `loadStudents()`: 加载学生列表
- `searchStudents()`: 搜索学生
- `updateStudentStatus(id, status)`: 更新学生状态（启用/禁用）
- `deleteStudent(id)`: 删除学生（需确认）
- `loadDoctors()`: 加载咨询师列表
- `searchDoctors()`: 搜索咨询师
- `updateDoctorStatus(id, status)`: 更新咨询师状态（审核/上下架）
- `deleteDoctor(id)`: 删除咨询师（需确认）
- `loadAnnouncements()`: 加载公告列表
- `showCreateAnnouncementForm()`: 显示创建公告表单
- `createAnnouncement()`: 创建新公告
- `editAnnouncement(announcement)`: 编辑公告
- `updateAnnouncement()`: 更新公告
- `updateAnnouncementStatus(id, status)`: 更新公告状态（发布/撤回）
- `deleteAnnouncement(id)`: 删除公告（需确认）

## 配置信息

### 访问地址
- **登录入口**: `http://localhost:5173/admin/login`
- **管理后台**: `http://localhost:5173/admin/home`

### 默认管理员账号
- **用户名**: admin
- **密码**: admin123

> ⚠️ **重要安全提示**: 
> - 生产环境必须立即修改默认密码！
> - 建议使用强密码（大小写字母+数字+符号，至少12位）
> - 定期更换管理员密码
> - 不要在公开场合泄露管理员登录入口

### 数据库初始化
首次使用前需执行 `init_admin.sql` 脚本创建管理员账号和测试公告：
```bash
# 方式1: 命令行执行
mysql -u your_username -p your_database < init_admin.sql

# 方式2: MySQL客户端执行
source /path/to/init_admin.sql;
```

### 数据库表结构
```sql
-- 管理员表
CREATE TABLE admin (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  username VARCHAR(50) UNIQUE NOT NULL COMMENT '管理员用户名',
  password VARCHAR(255) NOT NULL COMMENT '密码（BCrypt加密）',
  email VARCHAR(100) COMMENT '邮箱',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间'
) COMMENT '管理员表';

-- 公告表
CREATE TABLE announcement (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  title VARCHAR(200) NOT NULL COMMENT '公告标题',
  content TEXT NOT NULL COMMENT '公告内容',
  type VARCHAR(50) DEFAULT 'notice' COMMENT '公告类型：notice-通知 urgent-紧急 maintenance-维护',
  priority INT DEFAULT 0 COMMENT '优先级：数字越大越靠前',
  status TINYINT DEFAULT 0 COMMENT '状态：0-草稿 1-已发布',
  published_at DATETIME COMMENT '发布时间',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  INDEX idx_status_priority (status, priority DESC),
  INDEX idx_published_at (published_at DESC)
) COMMENT '系统公告表';
```

## API接口文档

### 1. 管理员登录
**请求**: `POST /api/admin/auth/login`  
**请求体**:
```json
{
  "username": "admin",
  "password": "admin123"
}
```
**响应**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "adminInfo": {
      "id": 1,
      "username": "admin",
      "email": "admin@example.com",
      "createdAt": "2024-01-01T00:00:00"
    }
  }
}
```

### 2. 获取管理员信息
**请求**: `GET /api/admin/profile`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 1,
    "username": "admin",
    "email": "admin@example.com",
    "createdAt": "2024-01-01T00:00:00"
  }
}
```

### 3. 获取所有学生
**请求**: `GET /api/admin/students`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 1,
      "username": "student001",
      "studentNumber": "2024001",
      "email": "student@example.com",
      "gender": 0,
      "phone": "13800138000",
      "status": 1,
      "createdAt": "2024-01-01T00:00:00"
    }
  ]
}
```

### 4. 搜索学生
**请求**: `GET /api/admin/students/search?keyword=student001`  
**Headers**: `Authorization: Bearer {token}`  
**响应**: 同"获取所有学生"

### 5. 更新学生状态
**请求**: `PUT /api/admin/students/{id}/status`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "status": 0
}
```
**响应**:
```json
{
  "code": 200,
  "message": "更新成功"
}
```

### 6. 删除学生
**请求**: `DELETE /api/admin/students/{id}`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "message": "删除成功"
}
```

### 7. 获取所有咨询师
**请求**: `GET /api/admin/doctors`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 1,
      "userId": 100,
      "name": "张医生",
      "title": "主任医师",
      "specialties": "焦虑、抑郁",
      "consultationFee": 200.00,
      "status": 1,
      "tags": ["焦虑", "抑郁"],
      "createdAt": "2024-01-01T00:00:00"
    }
  ]
}
```

### 8. 搜索咨询师
**请求**: `GET /api/admin/doctors/search?keyword=张医生`  
**Headers**: `Authorization: Bearer {token}`  
**响应**: 同"获取所有咨询师"

### 9. 更新咨询师状态
**请求**: `PUT /api/admin/doctors/{id}/status`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "status": 1
}
```
**说明**: status = 0（下架）、1（上架）、2（待审核）  
**响应**:
```json
{
  "code": 200,
  "message": "更新成功"
}
```

### 10. 删除咨询师
**请求**: `DELETE /api/admin/doctors/{id}`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "message": "删除成功"
}
```

### 11. 获取所有公告
**请求**: `GET /api/admin/announcements`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 1,
      "title": "系统维护通知",
      "content": "本系统将于...",
      "type": "maintenance",
      "priority": 100,
      "status": 1,
      "publishedAt": "2024-12-24T10:00:00",
      "createdAt": "2024-12-23T15:00:00"
    }
  ]
}
```

### 12. 获取公告详情
**请求**: `GET /api/admin/announcements/{id}`  
**响应**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": {
    "id": 1,
    "title": "系统维护通知",
    "content": "本系统将于2024年12月25日进行系统维护...",
    "type": "maintenance",
    "priority": 100,
    "status": 1,
    "publishedAt": "2024-12-24T10:00:00",
    "createdAt": "2024-12-23T15:00:00",
    "updatedAt": "2024-12-23T16:00:00"
  }
}
```

### 13. 创建公告
**请求**: `POST /api/admin/announcements`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "title": "新年公告",
  "content": "祝大家新年快乐！",
  "type": "notice",
  "priority": 50,
  "status": 1
}
```
**响应**:
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 5,
    "title": "新年公告",
    "content": "祝大家新年快乐！",
    "type": "notice",
    "priority": 50,
    "status": 1,
    "publishedAt": "2024-12-24T16:00:00",
    "createdAt": "2024-12-24T16:00:00"
  }
}
```

### 14. 更新公告
**请求**: `PUT /api/admin/announcements/{id}`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "title": "更新后的标题",
  "content": "更新后的内容",
  "type": "notice",
  "priority": 60,
  "status": 1
}
```
**响应**:
```json
{
  "code": 200,
  "message": "更新成功",
  "data": {...}
}
```

### 15. 更新公告状态
**请求**: `PUT /api/admin/announcements/{id}/status`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "status": 0
}
```
**说明**: status = 0（草稿/撤回）、1（发布）  
**响应**:
```json
{
  "code": 200,
  "message": "更新成功"
}
```

### 16. 删除公告
**请求**: `DELETE /api/admin/announcements/{id}`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "message": "删除成功"
}
```

### 17. 获取已发布公告（公共接口）
**请求**: `GET /api/admin/public/announcements?limit=5`  
**说明**: 无需认证，学生端首页调用  
**响应**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 1,
      "title": "系统维护通知",
      "content": "...",
      "type": "maintenance",
      "priority": 100,
      "publishedAt": "2024-12-24T10:00:00"
    }
  ]
}
```

## 使用流程

### 管理员登录流程
1. **访问登录页**: 访问 `http://localhost:5173/admin/login`
2. **输入凭据**: 输入用户名和密码
3. **提交登录**: 点击登录按钮
4. **验证成功**: 后端验证通过，返回JWT token
5. **跳转后台**: 自动跳转到管理后台首页

### 学生管理流程
1. **查看列表**: 点击"学生管理"菜单，查看所有学生
2. **搜索学生**: 输入关键词（用户名/学号/邮箱）搜索
3. **状态管理**: 点击"启用/禁用"按钮切换账号状态
4. **删除账号**: 点击"删除"按钮 → 确认对话框 → 物理删除

### 咨询师管理流程
1. **查看列表**: 点击"医生管理"菜单，查看所有咨询师
2. **搜索咨询师**: 输入关键词（姓名/职称/专长）搜索
3. **审核医生**: 对状态为"待审核"的医生点击"通过"按钮
4. **上下架**: 点击"上架/下架"按钮控制医生是否对学生可见
5. **删除账号**: 点击"删除"按钮 → 确认对话框 → 物理删除

### 公告管理流程
1. **查看列表**: 点击"公告管理"菜单，查看所有公告
2. **创建公告**: 点击"新建公告" → 填写标题、内容、类型、优先级 → 提交
3. **编辑公告**: 点击"编辑"按钮 → 修改内容 → 保存更新
4. **发布/撤回**: 点击"发布/撤回"按钮切换公告状态
5. **删除公告**: 点击"删除"按钮 → 确认对话框 → 删除记录

## 注意事项

### 安全注意事项
1. **强制修改默认密码**: 首次登录后立即修改默认密码
2. **隐藏登录入口**: 不要在公开文档中暴露管理员登录URL
3. **定期更换密码**: 建议每3-6个月更换一次管理员密码
4. **使用强密码**: 至少12位，包含大小写字母、数字和符号
5. **限制访问**: 仅在安全网络环境下访问管理后台

### 操作注意事项
1. **删除操作**: 删除学生/咨询师是物理删除，不可恢复，操作前务必确认
2. **状态管理**: 禁用学生账号后，该学生无法登录系统
3. **下架咨询师**: 下架后的咨询师不会在学生端显示，但已有预约不受影响
4. **公告发布**: 只有状态为"已发布"的公告才会在学生端首页显示
5. **优先级设置**: 数字越大的公告越靠前显示

### 数据安全
1. **备份数据**: 在执行删除操作前建议备份数据库
2. **日志记录**: 系统会记录所有管理员操作日志
3. **权限隔离**: 管理员只能管理学生和咨询师，不能访问聊天记录细节
4. **敏感信息**: 不要在公告中发布包含个人隐私的信息

## 业务规则

### 账号状态规则
**学生状态**:
- 0: 禁用（无法登录，所有功能不可用）
- 1: 正常（可正常使用所有功能）

**咨询师状态**:
- 0: 下架（学生端不可见，不能接受新预约）
- 1: 上架（学生端可见，可接受新预约）
- 2: 待审核（新注册医生默认状态，需管理员审核）

### 公告状态规则
- 0: 草稿（仅管理员可见，不在学生端展示）
- 1: 已发布（学生端首页展示，按优先级和发布时间排序）

### 公告类型说明
- `notice`: 普通通知（蓝色标识）
- `urgent`: 紧急公告（红色标识）
- `maintenance`: 维护通知（黄色标识）

### 公告显示规则
- 学生端首页默认显示最新5条已发布公告
- 按优先级降序排列，优先级相同则按发布时间降序
- 撤回公告（status=0）立即从学生端消失

## 常见问题

### Q1: 忘记管理员密码怎么办？
A: 需要直接操作数据库重置密码。可以使用以下SQL更新为新密码（需先用BCrypt加密）：
```sql
UPDATE admin SET password = '$2a$10$新的BCrypt密码哈希' WHERE username = 'admin';
```

### Q2: 可以创建多个管理员吗？
A: 当前系统支持单管理员模式。如需多管理员，需在数据库中手动插入新的管理员记录。

### Q3: 删除学生/咨询师会删除哪些数据？
A: 物理删除会删除用户表记录，但相关的预约记录、聊天记录等会保留（设计为软删除或保留关联数据）。

### Q4: 公告内容支持富文本吗？
A: 当前版本公告内容为纯文本。如需富文本，需前后端同时升级（后端使用TEXT类型已支持，前端需富文本编辑器）。

### Q5: 如何批量操作用户？
A: 当前版本不支持批量操作。需要逐个点击操作按钮。未来版本可考虑添加批量选择功能。

### Q6: 管理员可以查看用户的聊天记录吗？
A: 出于隐私保护，管理员无权查看学生与咨询师的聊天记录内容。仅能管理账号状态。

## 未来改进

1. **权限分级**: 支持多级管理员（超级管理员、普通管理员）
2. **批量操作**: 支持批量启用/禁用/删除用户
3. **操作日志**: 完善的管理员操作日志记录与查询
4. **数据统计**: 添加用户统计、预约统计、活跃度分析等数据看板
5. **导出功能**: 支持导出用户列表、公告列表为Excel
6. **富文本编辑器**: 公告内容支持富文本、图片、链接
7. **定时发布**: 支持公告定时自动发布
8. **消息推送**: 公告发布后自动推送给学生
9. **回收站**: 软删除用户，可恢复
10. **审计追踪**: 完整的用户操作审计追踪系统

---

**开发完成时间**: 2024年  
**技术栈**: Spring Boot 3.x + Vue 3 + MySQL  
**安全级别**: ⚠️ 高权限模块，需严格保密  
**开发者**: GitHub Copilot
