# 心理咨询系统需求说明（已实现范围）

- 文档版本：v1.2（2025-12-24）
- 适用对象：产品、设计、前端（Vue3）、后端（Spring Boot）、测试、运维
- 技术栈：Vue3 + Spring Boot + MySQL
- 说明：本稿仅保留当前代码中已实现或可用的能力，未开发的需求已移除或在“当前不包含”中标注。

## 1. 系统目标
- 面向大学生场景，提供 AI 自助心理对话和真实咨询师的线上沟通与线下预约。
- 建立基础账号体系、会话体系、预约体系与公告发布，提供简单的风险提示与心理测评。

## 2. 角色与权限
- 学生（student）：注册/登录后可使用 AI 对话、浏览咨询师、预约、与咨询师文字聊天、进行心理测评、查看公告、维护个人资料与密码。
- 咨询师（doctor）：注册/登录后可维护个人资料与排班，处理预约（确认/拒绝/完成）、与学生文字聊天、查看今日统计。
- 管理员（admin）：登录后可管理学生/咨询师账号状态，管理公告内容并下发到学生端。

## 3. 功能范围

### 3.1 通用能力
- 账号与鉴权：学生按学号/邮箱注册，咨询师手机号/邮箱注册；登录返回 JWT（在 Authorization: Bearer 中携带角色信息）。
- 文件上传：头像与通用文件上传（仅图片 JPG/PNG/GIF/WEBP，≤5MB），分别落盘到 uploads/avatars/students 或 uploads/avatars/doctors，并更新头像地址。
- 公告：管理员发布的公告可在学生端查看，按发布时间倒序，支持状态上下线。

### 3.2 学生端（Web/H5 响应式）
- 个人信息：查看/编辑昵称、学院、班级、手机号、邮箱、头像；修改密码。
- AI 心理对话：
	- 创建/查看/删除会话，查看历史消息。
	- 纯文本对话，调用 DeepSeek API；接口异常时有本地兜底回复。
	- 风险提示：基于关键词的风险等级（low/medium/high），返回建议操作列表。
- 咨询师与预约：
	- 浏览/搜索咨询师，查看详情（擅长标签、年限、简介等）。
	- 查看咨询师周排班（按周偏移），选择可用时间段创建预约（状态初始为 pending）。
	- 查看本人预约列表；在非完成/已取消状态下可取消预约，释放时间段。
- 咨询师对话：
	- 获取/创建与咨询师的聊天会话，查看会话列表与消息。
	- 发送文本消息，支持未读数更新；仅一对一文字聊天。
- 心理测评：
	- 查看测评量表列表与题目；提交答案生成结果。
	- 查看个人测评记录与结果详情；查看完成数量统计。

### 3.3 咨询师端
- 登录与资料：手机号/邮箱+密码登录；查看/更新个人资料（姓名、职称、联系方式、简介、标签、方法、年限、头像）。
- 排班：查看当前或指定周排班；批量设置可用/不可用时间段，已被预约的时间段保留占用状态。
- 预约处理：
	- 查看今日预约、分页列表；按状态筛选。
	- 将预约从 pending 确认（可填写地点）或拒绝（填写原因），已确认的预约可标记完成（可填写备注）。
	- 支持查看学生基本信息，学生取消后释放时间段。
- 对话：查看与学生的会话列表、未读消息；查看消息记录并发送文本消息；支持待回复消息列表。
- 数据：今日统计（今日预约数、完成数、待处理预约数、未读消息数）。

### 3.4 管理端
- 登录：管理员账号登录（隐藏入口）。
- 学生管理：列表、关键字搜索、状态变更（启用/禁用）、删除。
- 咨询师管理：列表、关键字搜索、状态变更（启用/禁用）、删除。
- 公告管理：创建、编辑、上下线、删除公告；学生端获取已发布公告。

### 3.5 AI 与风险提示（已实现逻辑）
- 使用 DeepSeek（OpenAI 兼容接口）进行文本对话；温度 0.7、max_tokens 500。
- 系统提示词：女性咨询师“心语”，不做诊断/处方，主打共情与引导。
- 风险检测：基于内置危机/高风险关键词，输出 riskLevel=low|medium|high，并返回建议动作（热线、预约建议等）。

### 3.6 心理测评
- 量表管理：获取量表列表与详情（含题目）。
- 作答与结果：提交答案生成结果；查询个人记录、单条结果与完成数量统计。

## 4. 核心流程（当前可用）
- 学生 AI 对话：登录 → 创建/选择会话 → 发送文本 → 获取 AI 回复与风险提示 → 会话和消息持久化，可删除会话。
- 学生预约咨询：浏览/搜索咨询师 → 查看排班 → 选择时间段创建预约（pending） → 医生确认/拒绝 → 医生完成或学生取消。
- 医生排班与处理：设置周排班 → 接收预约 → 根据状态确认/拒绝/完成 → 排班自动占用/释放。
- 医生与学生对话：学生创建会话 → 双方文字消息往来，未读计数更新。
- 心理测评：学生选择量表 → 作答提交 → 生成结果并可回看记录。
- 公告：管理员发布 → 学生端按发布时间读取已发布公告。

## 5. 非功能与安全基线
- 接口风格：RESTful JSON，统一返回 ApiResponse/Result 包装；需在 Header 携带 Bearer Token。
- 身份与权限：JWT 中包含用户 ID 与角色前缀（student/doctor/admin），后端按角色路由到对应接口；未实现细粒度 RBAC。
- 密码安全：注册/修改密码时使用加密存储与校验。
- 上传校验：仅允许 JPG/PNG/GIF/WEBP，大小 ≤5MB，超限或空文件直接拒绝。
- 日志：关键操作有服务端日志记录（未提供前端审计界面）。
- 通信：默认 HTTP 接口，未实现 WebSocket；建议部署使用 HTTPS。

## 6. 当前不包含的能力（需二次开发方可提供）
- 语音输入/播报、视频/音视频通话、聊天图片或文件发送。
- 支付/结算、评价与评分、投诉申诉、爽约计费等交易类能力。
- 医生入驻审核与资质审批流程（当前医生注册即为可用，管理员仅可禁用/删除）。
- WebSocket/长连接实时推送、消息撤回、已读回执。
- 会话导出、数据删除/匿名化申请、敏感词配置后台。
- 地图/地址解析、线下导航、预约改期。

## 7. 交付物与运行边界
- 前端：Vue3（Vite），提供学生、咨询师、管理员三端页面，对应上述功能的调用。
- 后端：Spring Boot REST API，依赖 MySQL；AI 调用需配置 ai.api-key/base-url/model。
- 静态资源与上传文件存放于项目的 uploads 目录，需在部署时开放静态访问路径。
