# 心理测评模块使用说明

## 功能概述

心理测评模块提供标准化的心理健康量表测评功能，帮助学生了解自己的心理状态，包括焦虑、抑郁、压力等多个维度。

## 主要功能

### 1. 量表管理
- **量表列表**: 展示所有可用的心理测评量表
- **量表详情**: 查看量表说明、题目数量、预计时间
- **量表分类**: 按情绪、人际、压力等类别组织

### 2. 在线测评
- **题目展示**: 逐题展示测评题目
- **选项选择**: 单选或多选答题
- **进度提示**: 显示答题进度
- **自动计分**: 提交后自动计算得分

### 3. 结果查看
- **总分展示**: 显示测评总分
- **等级判定**: 根据得分判定结果等级（正常/轻度/中度/重度）
- **结果解读**: 提供详细的结果说明和建议
- **历史记录**: 查看所有历史测评记录

### 4. 统计功能
- **完成数量**: 统计已完成的测评次数
- **趋势分析**: 查看不同时间的测评结果变化（可选）

## 技术实现

### 后端实现

#### 1. 实体类
- `AssessmentScale`: 量表实体（id, name, description, category, questionCount, estimatedTime, instructions, status, sort）
- `AssessmentQuestion`: 题目实体（id, scaleId, questionNo, content, questionType, options, reverseScore, dimension）
- `AssessmentRecord`: 测评记录实体（id, userId, scaleId, totalScore, resultLevel, resultSummary, duration, status, startedAt, completedAt）
- `AssessmentAnswer`: 答题记录实体（id, recordId, questionId, answerValue, score）

#### 2. Mapper层
- `AssessmentScaleMapper`: 量表数据访问
  - `findAll()`: 查询所有量表
  - `findById(Long id)`: 根据ID查询
  - `findByStatus(int status)`: 根据状态查询

- `AssessmentQuestionMapper`: 题目数据访问
  - `findByScaleId(Long scaleId)`: 查询量表的所有题目
  - `findById(Long id)`: 根据ID查询题目

- `AssessmentRecordMapper`: 记录数据访问
  - `findByUserId(Long userId)`: 查询用户的测评记录
  - `findById(Long id)`: 根据ID查询记录
  - `insert(AssessmentRecord record)`: 新增记录
  - `update(AssessmentRecord record)`: 更新记录
  - `countByUserId(Long userId)`: 统计用户完成数

- `AssessmentAnswerMapper`: 答题记录数据访问
  - `findByRecordId(Long recordId)`: 查询记录的所有答案
  - `batchInsert(List<AssessmentAnswer> answers)`: 批量插入答案

#### 3. DTO类
- `AssessmentResultDTO`: 测评结果传输对象
- `SubmitAssessmentDTO`: 提交测评请求

#### 4. 服务层
`AssessmentService` 接口和 `AssessmentServiceImpl` 实现类：
- `getAllScales()`: 获取所有量表
- `getScaleById(Long scaleId)`: 根据ID获取量表
- `getQuestionsByScaleId(Long scaleId)`: 获取量表题目
- `submitAssessment(Long userId, SubmitAssessmentDTO dto)`: 提交测评
- `getUserRecords(Long userId)`: 获取用户测评记录
- `getResultById(Long recordId)`: 获取测评结果
- `getCompletedCount(Long userId)`: 获取完成数量统计

#### 5. 控制器
`AssessmentController` 提供测评 REST API：
- `GET /api/assessment/scales`: 获取量表列表
- `GET /api/assessment/scales/{scaleId}`: 获取量表详情
- `GET /api/assessment/scales/{scaleId}/questions`: 获取量表题目
- `POST /api/assessment/submit`: 提交测评
- `GET /api/assessment/records`: 获取测评记录
- `GET /api/assessment/results/{recordId}`: 获取测评结果
- `GET /api/assessment/stats`: 获取统计数据

### 前端实现

#### StudentAssessmentView.vue
测评列表页面：
**核心功能**:
- 量表列表卡片展示
- 显示题目数量、预计时间
- 显示完成次数统计
- 点击进入测评

**主要方法**:
- `loadScales()`: 加载量表列表
- `loadStats()`: 加载统计数据
- `startAssessment(scaleId)`: 开始测评
- `viewHistory()`: 查看历史记录

#### StudentAssessmentTestView.vue
在线测评页面：
**核心功能**:
- 题目逐题展示
- 选项单选或多选
- 进度条显示
- 提交测评

**主要方法**:
- `loadQuestions()`: 加载题目
- `selectOption(questionId, value)`: 选择选项
- `nextQuestion()`: 下一题
- `previousQuestion()`: 上一题
- `handleSubmit()`: 提交测评
- `calculateProgress()`: 计算进度

#### 测评结果页面
**核心功能**:
- 总分展示
- 等级判定（正常/轻度/中度/重度）
- 结果解读
- 建议措施
- 查看历史记录入口

**主要方法**:
- `loadResult(recordId)`: 加载结果
- `getLevel Color(level)`: 获取等级颜色
- `viewHistory()`: 查看历史

## 配置信息

### 数据库表结构
```sql
CREATE TABLE assessment_scale (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(100) NOT NULL COMMENT '量表名称',
  description TEXT COMMENT '量表描述',
  category VARCHAR(50) COMMENT '分类',
  question_count INT DEFAULT 0 COMMENT '题目数量',
  estimated_time INT DEFAULT 10 COMMENT '预计完成时间（分钟）',
  instructions TEXT COMMENT '答题说明',
  status TINYINT DEFAULT 1 COMMENT '状态：0-禁用 1-启用',
  sort INT DEFAULT 0 COMMENT '排序权重',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

CREATE TABLE assessment_question (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  scale_id BIGINT NOT NULL COMMENT '所属量表ID',
  question_no INT NOT NULL COMMENT '题目序号',
  content TEXT NOT NULL COMMENT '题目内容',
  question_type VARCHAR(20) DEFAULT 'single' COMMENT '题目类型：single-单选 multiple-多选',
  options JSON NOT NULL COMMENT '选项（JSON数组）',
  reverse_score TINYINT DEFAULT 0 COMMENT '是否反向计分：0-否 1-是',
  dimension VARCHAR(50) COMMENT '维度/因子',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_scale_id_question (scale_id),
  INDEX idx_question_no (scale_id, question_no)
);

CREATE TABLE assessment_record (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  user_id BIGINT NOT NULL COMMENT '用户ID',
  scale_id BIGINT NOT NULL COMMENT '量表ID',
  total_score INT COMMENT '总分',
  result_level VARCHAR(50) COMMENT '结果等级',
  result_summary TEXT COMMENT '结果摘要',
  duration INT COMMENT '答题用时（秒）',
  status VARCHAR(20) DEFAULT 'in_progress' COMMENT '状态：in_progress/completed',
  started_at DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '开始时间',
  completed_at DATETIME COMMENT '完成时间',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_user_id_record (user_id),
  INDEX idx_scale_id_record (scale_id)
);

CREATE TABLE assessment_answer (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  record_id BIGINT NOT NULL COMMENT '测评记录ID',
  question_id BIGINT NOT NULL COMMENT '题目ID',
  answer_value INT NOT NULL COMMENT '选择的答案值',
  score INT DEFAULT 0 COMMENT '得分',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_record_id (record_id),
  UNIQUE KEY uk_record_question (record_id, question_id)
);
```

## API接口文档

### 1. 获取量表列表
**请求**: `GET /api/assessment/scales`  
**响应**:
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "name": "焦虑自评量表(SAS)",
      "description": "评估焦虑症状的严重程度",
      "category": "情绪",
      "questionCount": 20,
      "estimatedTime": 10,
      "status": 1,
      "sort": 1
    }
  ]
}
```

### 2. 获取量表详情（含题目）
**请求**: `GET /api/assessment/scales/{scaleId}`  
**响应**:
```json
{
  "code": 200,
  "data": {
    "scale": {
      "id": 1,
      "name": "焦虑自评量表(SAS)",
      "description": "评估焦虑症状的严重程度",
      "questionCount": 20,
      "estimatedTime": 10,
      "instructions": "请根据您最近一周的实际感受选择最符合的选项"
    },
    "questions": [
      {
        "id": 1,
        "scaleId": 1,
        "questionNo": 1,
        "content": "我觉得比平常容易紧张或着急",
        "questionType": "single",
        "options": [
          {"label": "没有或很少时间", "value": 1},
          {"label": "有时", "value": 2},
          {"label": "大部分时间", "value": 3},
          {"label": "绝大部分或全部时间", "value": 4}
        ],
        "reverseScore": 0,
        "dimension": "焦虑"
      }
    ]
  }
}
```

### 3. 获取量表题目列表
**请求**: `GET /api/assessment/scales/{scaleId}/questions`  
**响应**:
```json
{
  "code": 200,
  "data": [
    {
      "id": 1,
      "questionNo": 1,
      "content": "我觉得比平常容易紧张或着急",
      "questionType": "single",
      "options": [...]
    }
  ]
}
```

### 4. 提交测评
**请求**: `POST /api/assessment/submit`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "scaleId": 1,
  "answers": [
    {"questionId": 1, "answerValue": 2},
    {"questionId": 2, "answerValue": 3},
    {"questionId": 3, "answerValue": 1}
  ],
  "duration": 480
}
```
**响应**:
```json
{
  "code": 200,
  "message": "测评提交成功",
  "data": {
    "recordId": 1001,
    "scaleName": "焦虑自评量表(SAS)",
    "totalScore": 45,
    "resultLevel": "轻度焦虑",
    "resultSummary": "您的焦虑水平处于轻度范围...",
    "completedAt": "2025-12-24T16:30:00"
  }
}
```

### 5. 获取测评记录列表
**请求**: `GET /api/assessment/records`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "data": [
    {
      "id": 1001,
      "scaleId": 1,
      "scaleName": "焦虑自评量表(SAS)",
      "totalScore": 45,
      "resultLevel": "轻度焦虑",
      "status": "completed",
      "completedAt": "2025-12-24T16:30:00"
    }
  ]
}
```

### 6. 获取测评结果详情
**请求**: `GET /api/assessment/results/{recordId}`  
**响应**:
```json
{
  "code": 200,
  "data": {
    "recordId": 1001,
    "scaleName": "焦虑自评量表(SAS)",
    "totalScore": 45,
    "resultLevel": "轻度焦虑",
    "resultSummary": "您的焦虑水平处于轻度范围，建议...",
    "completedAt": "2025-12-24T16:30:00",
    "duration": 480
  }
}
```

### 7. 获取统计数据
**请求**: `GET /api/assessment/stats`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "data": {
    "completedCount": 5,
    "totalScales": 8
  }
}
```

## 使用流程

1. **查看量表**: 进入心理测评页面 → 浏览可用量表列表
2. **开始测评**: 点击某个量表 → 阅读测评说明 → 开始答题
3. **答题**: 逐题阅读并选择选项 → 可前进或后退 → 完成所有题目
4. **提交**: 检查答题完整性 → 提交测评
5. **查看结果**: 自动跳转到结果页 → 查看总分和等级 → 阅读结果解读
6. **历史记录**: 进入我的测评 → 查看所有历史记录 → 点击查看详情

## 注意事项

1. **作答完整性**: 所有题目必须作答才能提交
2. **时间记录**: 系统会记录答题用时
3. **结果保存**: 测评结果永久保存，可随时查看
4. **重复测评**: 同一量表可以多次测评
5. **结果参考**: 测评结果仅供参考，不能替代专业诊断

## 业务规则

### 计分规则
1. **正向计分题**: 选项值直接作为得分
2. **反向计分题**: 使用公式转换（如：5-选项值）
3. **总分计算**: 所有题目得分相加
4. **等级判定**: 根据总分范围判定结果等级

### 结果等级判定（以SAS为例）
- 总分 < 50: 正常范围
- 50 ≤ 总分 < 60: 轻度焦虑
- 60 ≤ 总分 < 70: 中度焦虑
- 总分 ≥ 70: 重度焦虑

### 测评状态
- `in_progress`: 测评进行中（未提交）
- `completed`: 测评已完成

## 常见问题

### Q1: 测评结果准确吗？
A: 测评结果基于标准化量表，具有一定参考价值，但不能替代专业心理评估。如有严重心理问题，建议咨询专业心理医生。

### Q2: 可以修改已提交的答案吗？
A: 不可以。测评提交后不能修改。如需重新测评，可以再次完成同一量表。

### Q3: 多久可以重复测评？
A: 没有时间限制，但建议间隔至少1-2周，以便更准确地反映心理状态变化。

### Q4: 测评结果会被其他人看到吗？
A: 不会。测评结果仅用户本人可见，系统会严格保护隐私。

### Q5: 为什么有些题目是反向计分的？
A: 反向计分题用于检测作答的一致性和有效性，也能减少答题偏向。

## 量表说明

### 当前支持的量表（示例）
1. **焦虑自评量表(SAS)**
   - 题目数: 20题
   - 用途: 评估焦虑症状
   - 适用: 成年人

2. **抑郁自评量表(SDS)**
   - 题目数: 20题
   - 用途: 评估抑郁症状
   - 适用: 成年人

3. **压力感知量表(PSS)**
   - 题目数: 14题
   - 用途: 评估压力水平
   - 适用: 大学生及成年人

## 技术特点

1. **JSON存储选项**: 题目选项使用JSON格式存储，灵活扩展
2. **批量插入答案**: 使用批量插入提高性能
3. **唯一约束**: record_id + question_id 唯一，防止重复作答
4. **自动计分**: 后端自动计算得分和等级
5. **维度统计**: 支持按维度/因子分组统计（可选）

## 未来改进

1. 添加更多标准化量表
2. 支持自定义量表创建
3. 实现测评结果可视化图表
4. 添加测评提醒功能
5. 支持测评报告导出
6. 实现多维度结果分析
7. 添加测评建议推送

---

开发完成时间: 2024年  
技术栈: Spring Boot 3.x + Vue 3 + MySQL  
开发者: GitHub Copilot
