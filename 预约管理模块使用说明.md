# 预约管理模块使用说明

## 功能概述

预约管理模块实现学生与咨询师之间的线下会诊预约功能，包括预约创建、状态流转、排班联动等核心能力。

## 主要功能

### 1. 预约创建
- **选择咨询师**: 浏览咨询师列表并查看详情
- **查看排班**: 查看咨询师可预约时间段
- **填写信息**: 选择日期、时间段、咨询方式，填写问题描述
- **提交预约**: 创建预约记录，状态为 pending（待确认）

### 2. 预约状态流转
- **pending（待确认）**: 学生提交后的初始状态
- **confirmed（已确认）**: 咨询师确认后的状态
- **completed（已完成）**: 咨询完成后的状态
- **cancelled（已取消）**: 学生或咨询师取消的状态

### 3. 学生端功能
- **查看预约列表**: 查看所有预约记录
- **预约详情**: 查看预约的详细信息
- **取消预约**: 取消待确认或已确认的预约

### 4. 咨询师端功能
- **今日预约**: 快速查看今日预约
- **预约列表**: 分页查看所有预约，支持状态筛选
- **确认预约**: 确认待处理预约，可填写咨询地点
- **拒绝预约**: 拒绝预约并说明原因
- **完成预约**: 标记预约完成，可填写咨询备注

### 5. 排班联动
- **自动占用**: 预约确认后，对应时间段自动标记为已占用
- **自动释放**: 预约取消后，对应时间段自动释放为可预约
- **冲突检测**: 创建预约前检查时间段是否已被占用

## 技术实现

### 后端实现

#### 1. 实体类
- `Appointment`: 预约实体（id, studentId, doctorId, appointmentDate, timeSlot, type, status, location, description, rejectReason, notes, createdAt, updatedAt, deletedAt）

#### 2. Mapper层
- `AppointmentMapper`: 预约数据访问
  - `findById(Long id)`: 根据ID查询
  - `findByStudentId(Long studentId)`: 查询学生的预约列表
  - `findTodayByDoctorId(Long doctorId, LocalDate today)`: 查询咨询师今日预约
  - `findByDoctorIdWithPage(...)`: 分页查询咨询师预约
  - `countByDoctorId(Long doctorId, String status)`: 统计预约数量
  - `countTodayByDoctorId(...)`: 统计今日预约数
  - `countTodayCompletedByDoctorId(...)`: 统计今日完成数
  - `countAllPendingByDoctorId(Long doctorId)`: 统计待处理预约数
  - `checkTimeSlotBooked(...)`: 检查时间段是否已被预约
  - `insert(Appointment appointment)`: 新增预约
  - `confirm(Long id, String location)`: 确认预约
  - `reject(Long id, String reason)`: 拒绝预约
  - `complete(Long id, String notes)`: 完成预约
  - `updateStatus(Long id, String status)`: 更新预约状态

#### 3. DTO类
- `AppointmentDTO`: 预约信息传输对象
- `CreateAppointmentRequest`: 创建预约请求
- `AppointmentListDTO`: 预约列表（分页）
- `ConfirmAppointmentRequest`: 确认预约请求
- `RejectAppointmentRequest`: 拒绝预约请求
- `CompleteAppointmentRequest`: 完成预约请求

#### 4. 服务层
`AppointmentService` 接口和 `AppointmentServiceImpl` 实现类：
- `createAppointment(CreateAppointmentRequest request, Long studentId)`: 创建预约
- `getAppointmentById(Long appointmentId)`: 根据ID获取预约
- `getStudentAppointments(Long studentId)`: 获取学生的预约列表
- `getTodayAppointments(Long doctorId)`: 获取咨询师今日预约
- `getAppointments(Long doctorId, String status, int page, int pageSize)`: 分页获取预约
- `confirmAppointment(Long appointmentId, Long doctorId, String location)`: 确认预约
- `rejectAppointment(Long appointmentId, Long doctorId, String reason)`: 拒绝预约
- `completeAppointment(Long appointmentId, Long doctorId, String notes)`: 完成预约
- `cancelAppointment(Long appointmentId, Long studentId)`: 取消预约

#### 5. 控制器
`UserController` 提供学生端预约 API：
- `POST /api/user/appointments`: 创建预约
- `GET /api/user/appointments`: 获取我的预约列表
- `PUT /api/user/appointments/{id}/cancel`: 取消预约

`DoctorController` 提供咨询师端预约 API：
- `GET /api/doctor/appointments/today`: 今日预约
- `GET /api/doctor/appointments`: 预约列表（分页）
- `PUT /api/doctor/appointments/{id}/confirm`: 确认预约
- `PUT /api/doctor/appointments/{id}/reject`: 拒绝预约
- `PUT /api/doctor/appointments/{id}/complete`: 完成预约

### 前端实现

#### StudentDoctorProfileView.vue
咨询师详情页：
**核心功能**:
- 展示咨询师详细信息
- 查看周排班
- 选择可用时间段
- 提交预约

**主要方法**:
- `loadDoctorInfo()`: 加载咨询师信息
- `loadSchedule(weekOffset)`: 加载排班
- `selectSlot(date, slot)`: 选择时间段
- `handleAppointment()`: 提交预约

#### StudentAppointmentsView.vue
学生预约列表页：
**核心功能**:
- 展示所有预约记录
- 状态标识（待确认、已确认、已完成、已取消）
- 预约详情展示
- 取消预约操作

**主要方法**:
- `loadAppointments()`: 加载预约列表
- `handleCancel(id)`: 取消预约
- `getStatusText(status)`: 获取状态文本
- `getStatusColor(status)`: 获取状态颜色

#### DoctorAppointmentsView.vue
咨询师预约管理页：
**核心功能**:
- 预约列表展示（分页）
- 状态筛选
- 确认/拒绝/完成操作
- 学生信息展示

**主要方法**:
- `loadAppointments(page, status)`: 加载预约列表
- `handleConfirm(id)`: 确认预约
- `handleReject(id)`: 拒绝预约
- `handleComplete(id)`: 完成预约
- `filterByStatus(status)`: 状态筛选

## 配置信息

### 数据库表结构
```sql
CREATE TABLE appointment (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  student_id BIGINT NOT NULL COMMENT '学生ID',
  doctor_id BIGINT NOT NULL COMMENT '咨询师ID',
  appointment_date DATE NOT NULL COMMENT '预约日期',
  time_slot VARCHAR(10) NOT NULL COMMENT '时间段',
  type VARCHAR(20) DEFAULT 'offline' COMMENT '咨询方式：offline-线下 online-线上',
  status VARCHAR(20) DEFAULT 'pending' COMMENT '状态：pending/confirmed/completed/cancelled',
  location VARCHAR(200) COMMENT '咨询地点',
  description TEXT COMMENT '问题描述',
  reject_reason VARCHAR(500) COMMENT '拒绝原因',
  notes TEXT COMMENT '咨询备注',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted_at DATETIME COMMENT '删除时间（软删除）',
  INDEX idx_student_id (student_id),
  INDEX idx_doctor_id (doctor_id),
  INDEX idx_appointment_date (appointment_date),
  INDEX idx_status (status)
);
```

## API接口文档

### 1. 创建预约
**请求**: `POST /api/user/appointments`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "doctorId": 1,
  "date": "2025-12-25",
  "timeSlot": "14:00",
  "type": "offline",
  "description": "最近压力很大，想咨询一下"
}
```
**响应**:
```json
{
  "code": 200,
  "message": "预约创建成功",
  "data": {
    "id": 10023,
    "studentId": 1,
    "doctorId": 1,
    "doctorName": "李华",
    "doctorAvatar": "/uploads/avatars/doctors/avatar_1.jpg",
    "date": "2025-12-25",
    "timeSlot": "14:00",
    "type": "offline",
    "status": "pending",
    "description": "最近压力很大，想咨询一下",
    "createdAt": "2025-12-24T10:30:00"
  }
}
```

### 2. 获取我的预约列表
**请求**: `GET /api/user/appointments`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "message": "获取成功",
  "data": [
    {
      "id": 10023,
      "doctorId": 1,
      "doctorName": "李华",
      "doctorAvatar": "/uploads/avatars/doctors/avatar_1.jpg",
      "date": "2025-12-25",
      "timeSlot": "14:00",
      "type": "offline",
      "status": "confirmed",
      "location": "心理咨询中心101室",
      "description": "最近压力很大，想咨询一下",
      "createdAt": "2025-12-24T10:30:00"
    }
  ]
}
```

### 3. 取消预约
**请求**: `PUT /api/user/appointments/{id}/cancel`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "message": "预约已取消",
  "data": {
    "id": 10023,
    "status": "cancelled"
  }
}
```

### 4. 获取今日预约（咨询师）
**请求**: `GET /api/doctor/appointments/today`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "data": [
    {
      "id": 10023,
      "studentId": 1,
      "studentName": "张同学",
      "studentAvatar": "/uploads/avatars/students/avatar_1.jpg",
      "studentCollege": "计算机学院",
      "date": "2025-12-24",
      "timeSlot": "14:00",
      "type": "offline",
      "status": "confirmed",
      "location": "心理咨询中心101室",
      "description": "最近压力很大，想咨询一下"
    }
  ]
}
```

### 5. 获取预约列表（咨询师，分页）
**请求**: `GET /api/doctor/appointments?status=pending&page=1&pageSize=10`  
**Headers**: `Authorization: Bearer {token}`  
**响应**:
```json
{
  "code": 200,
  "data": {
    "list": [...],
    "total": 25,
    "page": 1,
    "pageSize": 10
  }
}
```

### 6. 确认预约（咨询师）
**请求**: `PUT /api/doctor/appointments/{id}/confirm`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "location": "心理咨询中心101室"
}
```
**响应**:
```json
{
  "code": 200,
  "data": {
    "id": 10023,
    "status": "confirmed",
    "location": "心理咨询中心101室"
  }
}
```

### 7. 拒绝预约（咨询师）
**请求**: `PUT /api/doctor/appointments/{id}/reject`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "reason": "该时间段临时有事，抱歉"
}
```
**响应**:
```json
{
  "code": 200,
  "data": {
    "id": 10023,
    "status": "rejected",
    "rejectReason": "该时间段临时有事，抱歉"
  }
}
```

### 8. 完成预约（咨询师）
**请求**: `PUT /api/doctor/appointments/{id}/complete`  
**Headers**: `Authorization: Bearer {token}`  
**请求体**:
```json
{
  "notes": "咨询进展顺利，建议继续巩固"
}
```
**响应**:
```json
{
  "code": 200,
  "data": {
    "id": 10023,
    "status": "completed",
    "notes": "咨询进展顺利，建议继续巩固"
  }
}
```

## 使用流程

### 学生端流程
1. **浏览咨询师**: 进入咨询师列表 → 搜索/筛选 → 查看咨询师详情
2. **查看排班**: 在咨询师详情页查看可预约时间
3. **创建预约**: 选择日期和时间段 → 填写问题描述 → 提交预约
4. **等待确认**: 预约状态为 pending，等待咨询师确认
5. **查看结果**: 进入我的预约 → 查看预约状态（已确认/已拒绝）
6. **到访咨询**: 按预约时间到达咨询地点
7. **取消预约**: 如需取消，在预约详情页点击取消（仅限 pending 和 confirmed 状态）

### 咨询师端流程
1. **查看预约**: 工作台查看今日预约或进入预约管理查看全部
2. **处理预约**: 
   - 确认：填写咨询地点 → 提交确认
   - 拒绝：填写拒绝原因 → 提交拒绝
3. **进行咨询**: 按预约时间为学生提供咨询服务
4. **完成预约**: 咨询结束后 → 填写咨询备注 → 标记完成
5. **统计更新**: 完成预约后自动更新咨询时长和帮助人数

## 注意事项

1. **时间段冲突**: 创建预约前会检查时间段是否已被占用
2. **状态限制**: 
   - 只能取消 pending 或 confirmed 状态的预约
   - 只能确认 pending 状态的预约
   - 只能完成 confirmed 状态的预约
3. **排班联动**: 
   - 确认预约后时间段自动标记为已占用
   - 取消预约后时间段自动释放
4. **权限校验**: 
   - 学生只能查看和操作自己的预约
   - 咨询师只能查看和操作自己的预约
5. **软删除**: 预约记录支持软删除，不会物理删除数据

## 业务规则

### 创建预约规则
1. 必须选择可用的时间段（available=true, booked=false）
2. 同一咨询师同一时间段不能重复预约
3. 咨询方式支持 offline（线下）和 online（线上）
4. 问题描述为可选项

### 状态流转规则
```
pending（待确认）
  ├─→ confirmed（已确认）[咨询师确认]
  ├─→ rejected（已拒绝）[咨询师拒绝]
  └─→ cancelled（已取消）[学生取消]

confirmed（已确认）
  ├─→ completed（已完成）[咨询师完成]
  └─→ cancelled（已取消）[学生取消]
```

### 排班联动规则
1. **确认预约时**:
   - 调用 `doctorScheduleService.markSlotAsBooked()` 标记时间段为已占用
   - 更新 `doctor_schedule` 表的 `booked` 字段为 1

2. **取消预约时**:
   - 调用 `doctorScheduleService.markSlotAsAvailable()` 释放时间段
   - 更新 `doctor_schedule` 表的 `booked` 字段为 0

3. **完成预约时**:
   - 更新咨询师统计数据（帮助人数+1，咨询时长+1小时）
   - 时间段保持已占用状态（历史记录）

## 常见问题

### Q1: 为什么无法创建预约？
A: 可能原因：
- 选择的时间段不可用（available=false）
- 选择的时间段已被预约（booked=true）
- 咨询师状态不正常或不可预约

### Q2: 取消预约后能否再次预约同一时间段？
A: 可以。取消预约后时间段会自动释放，如果咨询师排班中该时间段仍然可用，则可以再次预约。

### Q3: 咨询师拒绝预约后学生会收到通知吗？
A: 当前版本未实现通知功能。学生需要进入"我的预约"查看预约状态和拒绝原因。

### Q4: 预约完成后能否修改备注？
A: 当前版本不支持修改已完成预约的备注。建议在标记完成前仔细填写。

## 未来改进

1. 添加预约提醒通知（短信/邮件/站内信）
2. 支持预约改期功能
3. 添加爽约处理机制
4. 实现预约评价功能
5. 支持在线视频咨询
6. 添加预约统计报表
7. 支持批量操作预约

---

开发完成时间: 2024年  
技术栈: Spring Boot 3.x + Vue 3 + MySQL  
开发者: GitHub Copilot
